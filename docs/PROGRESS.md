# Progression JudoVACapp

## Livré

| Étape | Contenu |
|-------|---------|
| 1–16 | Fondation → packaging → sidebar → multi-badges → preview → test sync |
| 17 | Sync client update/delete (file + Socket.IO) |
| 18 | Nom d’événement dans la sidebar |
| 19 | Liste : sélection + export PDF / impression (serveur) |
| 20 | Icône app + packaging NSIS + .env packagé |

## Commandes

```bash
npm run dev
npm run test:sync
npm run icon:build
npm run dist
```
